export default function DemoModeBadge() {
  return (
    <div className="demo-mode-badge" title="Council runs on a deterministic local reasoning engine — no API keys, no network calls, no cost.">
      <span className="demo-mode-dot" />
      Demo Mode — Local Reasoning, Zero API Cost
    </div>
  );
}
